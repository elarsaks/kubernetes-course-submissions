apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-image-claim
  namespace: project
spec:
  storageClassName: project-image
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
