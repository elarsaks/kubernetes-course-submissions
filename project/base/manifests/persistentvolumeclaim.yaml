apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-image-claim
spec:
  # GKE dynamically provisions this volume through the cluster's CSI driver.
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
